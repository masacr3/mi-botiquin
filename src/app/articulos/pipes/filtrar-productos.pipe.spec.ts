import { FiltrarProductosPipe } from './filtrar-productos.pipe';

describe('FiltrarProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
